const cardLIst = ({ cards, name, imageUrl, id, originalText, artist }) => {

    return(
        <div className="card-list">
            <img>{ imageURL }</img>
            <h2></h2>
            <p></p>
        </div>
    )
}